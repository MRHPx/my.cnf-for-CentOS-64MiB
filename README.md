my.cnf-for-CentOS-64MiB
=======================

MySQL setting my.cnf for CentOS 64MiB

Configuration Path :
/etc/my.cnf

To Start MySQL Server :
/etc/init.d/mysqld start

To Stop MySQL Server :
/etc/init.d/mysqld stop

To Restart MySQL Server :
/etc/init.d/mysqld restart
